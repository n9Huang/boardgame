# 編修溝通模板

## 這次目標
-index 的目前只有一個cabin 的頁面，其它按鍵風格要一樣，首頁下方加上版本號，每次到這個頁面會看是不是最新
-新增計時器的頁面，中間一個圓形為倒數的時間，預設兩分鐘，最後十秒會有警示聲(並顯示紅色閃爍)，下方有快速設定和自定時間(30，60，__)，按下時間的圓形為重新計時。
-計分表，大老二，骰子的按鍵設為灰色，不可以按

## 目前狀態
- 已完成：
- 卡住點：
- 相關檔案：

## 方案（先寫這裡）
請先在此區提出「預計修改內容」，不要直接改程式。

### 預計修改
- 首頁按鍵視覺一致化
	- 針對 index 的工具按鍵樣式做一致化規則（尺寸、圓角、陰影、字級、可點擊狀態）。
	- 保留「可用」與「不可用」的視覺差異，但整體風格統一。
- 首頁版本號顯示
	- 在首頁底部新增版本號文字（例如：Zm.dd#，實際值依你當次發布日期/版次）。
	- 每次進入首頁時，檢查本地版本與服務工作執行緒版本是否一致（若有 sw.js）。
	- 若版本不一致，顯示「有新版本」提示並提供重新整理。
- 新增計時器頁面（timer）
	- 建立 timer.html（若已存在則改版）並提供中央圓形倒數 UI。
	- 預設倒數 120 秒（2 分鐘）。
	- 最後 10 秒觸發警示（紅色閃爍 + 警示聲）。
	- 下方提供快速時間按鍵：30 秒、60 秒、以及自定秒數輸入。
	- 點擊中央圓形時，重置並重新啟動本回合計時。
- 首頁停用按鍵（計分表/大老二/骰子）
	- 首頁將「計分表」「大老二」「骰子」設為灰色 disabled 狀態。
	- 停用時不可點擊、不可跳頁，並加入 aria-disabled 屬性與游標提示。
- 文件/連結邏輯同步
	- 若新增 timer 頁面，補上首頁連結邏輯說明（由首頁進入 timer）。
	- 若本次新增任何資料欄位，再同步更新 database.md（目前預估不需）。

### 方案檢查（由 Copilot 填寫）
- 可能問題：
	- Safari/iOS 對音效播放有互動限制，若未先互動可能無法自動播放警示聲。
	  - 展開說明：在 iPhone/Safari，瀏覽器通常要求「先有使用者手勢（點擊/觸控）」才能播放聲音。若倒數進入最後 10 秒時，使用者從未點過頁面，警示聲可能被瀏覽器阻擋。
	  - 對策：第一次進入計時器頁時，用「點一下啟用音效」或在第一次按開始時初始化 AudioContext；完成後最後 10 秒長音就能正常播放。
      -OK
	- 若 sw.js 尚未存在或版本管理未接入，首頁「是否最新」檢查需提供降級行為。
	  - 可以，這項先列為第二階段。第一階段先做首頁版本號顯示；第二階段再加 sw.js 版本比對與更新提示。
	- 灰色停用按鍵若仍保留 a 連結，需額外阻止點擊事件，避免仍可跳轉。
	  - 已確認：停用按鍵不可跳轉。實作上會同時做「視覺灰化 + pointer-events 停用 + JS 防呆攔截」。
- 風險：
	- 新增計時器動畫與音效後，低階手機可能有些微卡頓。
	- 版本檢查若設計為強制更新，可能影響正在操作中的使用者流程。
	  - 展開說明：例如使用者正在計時器倒數中，若此時被強制刷新頁面，倒數狀態會被中斷。建議先採「提示可更新」而非強制刷新。
- 替代作法：
	- 音效可改為「使用者首次點擊頁面後啟用 AudioContext」，確保各瀏覽器可播。
	- 版本檢查可先採「提示可更新」而非強制刷新。
	  - 會有「看到舊檔」風險，但可控。做法是：
	    - sw.js 偵測到新版本後顯示「有新版本」提示。
	    - 使用者按下「立即更新」時執行 reload，確保切到新版快取。
	    - 因此不會永久卡在舊版，只是把更新時機交給使用者選擇。
        >OK
	- 停用按鍵可改為 button 元素管理 disabled，語意更明確。
- 需你確認的數值/規則：
	- 自定時間輸入上限（建議 300 秒）與下限（建議 5 秒）。
	  - 已確認：上限 300 秒、下限 5 秒。
	- 警示聲播放方式：每秒一短嗶 or 單次長音。
	  - 已確認：單次長音。
	- 紅色閃爍頻率（建議 0.5 秒一次）。
	  - 已確認：0.5 秒一次。
	- 版本尾碼 # 的遞增規則（每天重置或持續累加）。
	  - 已確認：依「部署前檢查」規則執行（格式 Zm.dd#，# 由你的發布版次遞增）。


### 架構說明（由 Copilot 填寫）
- 架構模式：
	- 採「單頁入口 + 多工具子頁」模式：index 為導覽入口，各工具獨立 HTML。
	- 共用 UI Token（色彩/按鈕/字級）放在各頁 style 中保持一致，避免工具風格漂移。
	- 計時器採「狀態驅動」：idle/running/warning/finished 四狀態切換 UI 與音效。
- 主要流程/動作：
	- 首頁載入：渲染按鈕狀態 -> 顯示版本號 -> 檢查是否有新版本。
	- 點擊可用按鍵：導向對應工具頁（目前重點為 timer）。
	- 計時器操作：
		- 設定秒數（快速按鍵或自定） -> 啟動倒數。
		- 剩餘 <= 10 秒時進入 warning（紅色閃爍 + 警示聲）。
		- 倒數到 0 進入 finished。
		- 點擊中央圓形時，以目前設定秒數重新開始。

## 最後確認後再修改
- [ ] 已確認方案內容
- [ ] 同意開始修改

## 驗收標準
- [ ] 功能符合需求
- [ ] 不影響既有功能
- [ ] 有必要時補充測試方式

---

## 既有規範（原始內容整併）
### 一般規則
- 請都繁中回應，在下方方案處寫下要修改的地方。
- 先在方案寫下你要修改的東西，不要直接修改，不要修改其它區塊。
- 統整後一起修改。
- 幫我檢查方案的問題。
- 架構問題，請說明架構的模式和動作。
- 沒有說明的數值請列表和我確認

### 部署前檢查
- 更新首頁的版本號顯示（格式：Zm.dd#，m=月份代碼）。
- 同步更新 sw.js 第一行的 VERSION。
- 版本規則：
	- Year：26Z、27A、28B...（逐字母往下）
	- Month：1、2、3、4、5、6、7、8、9、0、J、Q
	- 備註：撲克牌邏輯 1~10、J、Q，無衝突。

### 資料欄位規則
- 有關於欄位名稱，請查 database.md。
- 若有新增欄位，也要同步加入文件。

### 頁面/連結規則
- 如果有新的頁面或連結，請說明邏輯。
- 新增或修改頁面版面時，請查 頁面架構規範.md（頁面分類、色彩/字型 Token、Safe Area 規則、新頁面模板）。