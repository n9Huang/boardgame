# 編修溝通模板

## 這次目標
- 我想要把INDEX的按鍵做更新，更好看，圖示有什麼建議嗎？
- 大老二.html可以幫我接到大老二按鈕嗎？可以改檔名
- 計時器頁面，功能是對的，但介面幫我整理更好看，並加入上一頁的按鍵(可以做成元件)
- 山中小屋也要上一頁
- index的頁面方手機上可以把safe area 拿掉
- 請給我骰子和計分表的建議

## 目前狀態
- 已完成：
  - 首頁按鍵圖示改為填色＋細節陰影風格，各工具加上專屬 hover 光暈色
  - 大老二.html 改名為 big2.html，首頁按鈕解除禁用
  - 計時器 / 山中小屋加上共用圓形返回按鈕元件
  - 首頁手機版（≤600px）移除 safe-area padding，桌機版不變
  - 新增 dice.html（骰子可自訂 1~6 顆，圓形卡片風格，點擊擲骰動畫）
  - 新增 scoreboard.html（格狀倍率計分表，玩家 2~6 人可自訂、列數不限、單擊疊加/±切換/長按歸零，localStorage 持久化）
  - 首頁版本號更新為 Z7.28.2，並同步 sw.js VERSION
- 卡住點：
  - 無
- 相關檔案：
  - `index.html` / `timer.html` / `cabin.html` / `big2.html`（原 大老二.html）/ `dice.html` / `scoreboard.html` / `sw.js`

## 下一個目標
- 

## 方案（先寫這裡）
請先在此區提出「預計修改內容」，不要直接改程式。

### 預計修改
- 首頁按鍵視覺更新
	- icon 改為「填色＋細節陰影」風格，取代目前純線條稿，啟用中按鈕加上輕微 hover 光暈。
	- 停用中按鈕維持灰階，但不做過度變形。
- 大老二串接
	- 將 `大老二.html` 改檔名為 `big2.html`（index.html 已連到 big2.html），並移除首頁按鈕的 `is-disabled` / `aria-disabled`。
- 計時器介面整理 + 返回元件
	- 左上角加圓形返回箭頭圖示按鈕，取代目前純文字「回首頁」連結。
	- 因為是純靜態頁面沒有 build 工具，返回按鈕以「共用 HTML/CSS 片段，各頁貼一份」的方式做元件化。
- 山中小屋加上一頁
	- cabin.html 套用同一款返回元件樣式。
- 首頁 Safe Area（僅手機版）
	- 只在手機（media query 判斷小螢幕）拿掉 `.container` 的 safe-area padding，電腦版維持不變。
- 骰子頁面
	- 新增 dice.html，可自訂 1~6 顆骰子，圓形卡片風格（比照計時器視覺），點擊或搖晃觸發擲骰動畫。
	- 首頁骰子按鈕解除禁用並連到 dice.html。
- 計分表頁面（格狀倍率計分器）
	- 新增/啟用 scoreboard.html，採「格狀倍率計分表」設計：
		- 玩家數 2~6 人可自訂（欄位對應各玩家，含新增玩家「+」）。
		- 列數不限，可用「+」新增列（每列代表一個回合/項目）。
		- 每列前方有「倍率標籤」，倍率數字可自訂輸入（例如 2、3、5...）。
		- 每個玩家格子單擊 = 疊加一次「+倍率分數」，格子內顯示目前疊加次數與合計。
		- 每個格子旁附一個小 +/- 切換鈕，切換後單擊變成疊加負分，格子右下角出現三角形切角提示為扣分模式。
		- 長按格子 = 重置該格為 0。
	- 首頁計分表按鈕解除禁用並連到 scoreboard.html。
	- 不沿用大老二.html 內既有的「終極計分 Pro」邏輯（走勢圖/勝率統計等進階功能先不做）。

### 方案檢查（由 Copilot 填寫）
- 可能問題：
	- 大老二.html 改檔名後，若有其他地方（書籤、外部連結）指向舊中文檔名會失效；目前檢查僅 index.html 有引用，風險低。
	- 骰子搖晃偵測（DeviceMotion）在部分瀏覽器需要使用者授權（尤其 iOS Safari），純點擊觸發較保險。
- 風險：
	- 計分表重做為簡化版，會失去大老二.html 現有版本的走勢圖/勝率統計等進階功能（之後若要仍可從大老二.html 移植）。
- 替代作法：
	- 骰子搖晃授權：可先只做「點擊擲骰」，之後再視需求加上加速度感應。
- 需你確認的數值/規則：
	- 返回按鈕位置與樣式：預計「左上角圓形箭頭」，是否可以？
    - OK
	- 計分表簡化版要保留哪些功能：目前預計只留「玩家自訂數量＋輸入分數＋歷史紀錄」，走勢圖/勝率統計等進階功能先不做，OK 嗎？
    - OK

### 架構說明（由 Copilot 填寫）
- 架構模式：
	- 維持「單頁入口 + 多工具子頁」：index.html 為導覽入口，各工具各自獨立 HTML，無共用 build 工具。
	- 返回按鈕做成「共用 HTML/CSS 片段」，以複製貼上的方式放進 timer.html、cabin.html（之後新頁也比照），非 import 元件。
	- 計分表（scoreboard.html）與骰子（dice.html）都採「狀態驅動＋localStorage 持久化」：頁面重整不遺失目前分數/設定（比照大老二.html 現有的 localStorage 模式）。
	- scoreboard.html 內部資料結構：`players[]`（2~6 人可增減）、`rows[]`（每列含 `multiplier` 倍率、`cells[]` 每格含 `count` 疊加次數與 `sign`（+/-））。
- 主要流程/動作：
	- 首頁載入：渲染按鈕視覺 → 判斷螢幕寬度套用/移除 safe-area padding → 顯示版本號。
	- 點擊可用按鈕：導向對應工具頁（timer / big2 / dice / scoreboard / cabin），子頁左上角返回按鈕可回首頁。
	- 骰子頁：設定顆數（1~6，可自訂）→ 點擊擲骰 → 顯示動畫與結果。
	- 計分表頁：
		- 新增/移除玩家欄位、新增列並設定該列倍率。
		- 單擊格子 → 疊加倍率分數（沿用目前 +/- 模式）；點格子旁 +/- 鈕切換該格加減模式（右下角三角形提示）；長按格子 → 歸零。
		- 每次操作即時更新該玩家總分並存入 localStorage。

## 最後確認後再修改
- [x] 已確認方案內容
- [x] 同意開始修改

## 驗收標準
- [ ] 功能符合需求
- [ ] 不影響既有功能
- [ ] 有必要時補充測試方式

---

## 既有規範（原始內容整併）
### 一般規則
- 請都繁中回應，在下方方案處寫下要修改的地方。
- 先在方案寫下你要修改的東西，不要直接修改，不要修改其它區塊。
- 統整後一起修改。
- 幫我檢查方案的問題。
- 架構問題，請說明架構的模式和動作。
- 沒有說明的數值請列表和我確認

### 部署前檢查
- 更新首頁的版本號顯示（格式：Zm.dd#，m=月份代碼）。
- 同步更新 sw.js 第一行的 VERSION。
- 版本規則：
	- Year：26Z、27A、28B...（逐字母往下）
	- Month：1、2、3、4、5、6、7、8、9、0、J、Q
	- 備註：撲克牌邏輯 1~10、J、Q，無衝突。

### 資料欄位規則
- 有關於欄位名稱，請查 database.md。
- 若有新增欄位，也要同步加入文件。

### 頁面/連結規則
- 如果有新的頁面或連結，請說明邏輯。
- 新增或修改頁面版面時，請查 頁面架構規範.md（頁面分類、色彩/字型 Token、Safe Area 規則、新頁面模板）。
-手機版不要safe area
