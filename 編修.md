# 編修溝通模板

## 這次目標
- 山中小屋的頁面調整
    -頁面的卡片想要長一點，大約多1.3倍
    -不知道是什麼原因，我在手機的第一張總是不會出現字
    -現在有記憶功能，所以在右上的設定裡，幫我加一個清空的按鈕
    -如果字太多，會有重疊的情況該怎麼辦，現在會重疊，並不能分開
## 目前狀態
- 已完成：
  - 卡片舞台放大約 1.3 倍（`.stage` height 300→560，並用 `min(..., 62dvh)` 防止矮螢幕被裁切）
  - 修正手機第一張卡片文字不出現的問題：打字效果不再被語音播放的 callback 卡住（iOS 首次呼叫 speechSynthesis 的 callback 有機率不觸發）
  - 設定面板新增「清空記錄」按鈕，會清掉記住的牌堆進度並重新洗牌
  - 修正文字重疊/被打斷問題：故事文字還沒打完前，不能提早點擊卡片觸發規則顯示
  - 版本號更新為 Z7.29.7，並同步 sw.js VERSION
  - 真正修正文字重疊：`.card-story` 原本設定 `flex:1` 會在空間不足時被壓扁但內容溢出畫到 `.rule-section` 上面；改成 `flex:none` 讓故事文字用自然高度，統一交給卡片本身的捲動處理
  - 版本號更新為 Z7.29.8，並同步 sw.js VERSION
  - 修正手機無法整頁上下捲動：`body` 原本設定 `overflow:hidden` 會把整頁鎖死，改成 `overflow-y:auto`（水平仍鎖住），內容過長時可以捲整頁
  - 版本號更新為 Z7.29.9，並同步 sw.js VERSION
- 卡住點：
  - 「重疊」問題若改完後仍出現，需要你截圖確認實際樣子才能再細部處理
- 相關檔案：
  - `cabin.html`

## 下一個目標
- 

## 方案（先寫這裡）
請先在此區提出「預計修改內容」，不要直接改程式。

### 預計修改
- 

### 方案檢查（由 Copilot 填寫）
- 可能問題：
- 風險：
- 替代作法：
- 需你確認的數值/規則：

### 架構說明（由 Copilot 填寫）
- 架構模式：
- 主要流程/動作：

## 最後確認後再修改
- [ ] 已確認方案內容
- [ ] 同意開始修改

## 驗收標準
- [ ] 功能符合需求
- [ ] 不影響既有功能
- [ ] 有必要時補充測試方式

---

## 既有規範（原始內容整併）
### 一般規則
- 請都繁中回應，在下方方案處寫下要修改的地方。
- 先在方案寫下你要修改的東西，不要直接修改，不要修改其它區塊。
- 統整後一起修改。
- 幫我檢查方案的問題。
- 架構問題，請說明架構的模式和動作。
- 沒有說明的數值請列表和我確認

### 部署前檢查
- 更新首頁的版本號顯示（格式：Zm.dd#，m=月份代碼）。
- 同步更新 sw.js 第一行的 VERSION。
- 版本規則：
	- Year：26Z、27A、28B...（逐字母往下）
	- Month：1、2、3、4、5、6、7、8、9、0、J、Q
	- 備註：撲克牌邏輯 1~10、J、Q，無衝突。

### 資料欄位規則
- 有關於欄位名稱，請查 database.md。
- 若有新增欄位，也要同步加入文件。

### 頁面/連結規則
- 如果有新的頁面或連結，請說明邏輯。
- 新增或修改頁面版面時，請查 頁面架構規範.md（頁面分類、色彩/字型 Token、Safe Area 規則、新頁面模板）。
- 手機版不要 safe area
