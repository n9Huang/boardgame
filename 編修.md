# 編修溝通模板

## 這次目標
- 記錄：我想確認，之前說的記錄說的是跳頁也會記錄嗎？
- safe area 要怎麼確定有呢？因為目前是沒有的
- 大老二 需要上一頁按鈕
- 我想確認一下 頁面架構
    index 
        容器(背圖在這裡) 
            -容器(標題按鈕，可以上下移，有標題，按鈕，版本)
        如圖，safe area 有問題

    計分器
        容器(上下左右擴展，不移動)
            -內容(新增和重新開始置底)

    計時器，骰子
        容器(上下左右擴展，不移動)
            -內容(中間容器可以不顯示邊框，按鈕置底)

- 計分器，計時器，骰子
    目前都有如圖一樣的上下框，不知道是什麼

- 桌面版，我要怎麼樣設定更新呢？

## 目前狀態
- 已完成：
  - 大老二（big2.html）加上返回按鈕（淡色主題，符合原本淺色頁面風格）
  - 計時器 / 骰子 / 計分表 拿掉中間半透明卡片框，內容直接鋪在頁面背景上；操作按鈕改為固定貼底（含 safe-area-inset-bottom）
  - 山中小屋（cabin.html）新增 localStorage 記住牌堆剩餘順序，語音設定維持每次預設
  - 六個頁面（index/timer/dice/scoreboard/cabin/big2）viewport 加上 `maximum-scale=1, user-scalable=no` 禁止縮放，並加 `overscroll-behavior: none` 避免整頁拖動彈跳
  - 記錄消失問題已排查：確認是使用者 iOS Safari「封鎖所有 Cookie」設定連帶擋掉 localStorage，關閉後恢復正常，非程式問題
  - 版本號更新為 Z7.29.1，並同步 sw.js VERSION
  - 修正計分表減分機制：拿掉「+/− 切換模式」，改成獨立的「−」按鈕（按一次扣一次，不影響單擊格子的加分邏輯），並修掉按 +/− 會連帶多算一次分數的 bug；同時保留舊存檔（count/sign 格式）自動轉換相容
  - 版本號更新為 Z7.29.2，並同步 sw.js VERSION
  - 依你提供的騎士公仔圖（icon-source.png）產出 favicon（16/32px）、apple-touch-icon（180px）、PWA icon（192/512px），新增 manifest.json，六個頁面都接上對應 `<link>` 標籤
  - 版本號更新為 Z7.29.3，並同步 sw.js VERSION
- 卡住點：
  - 無
- 相關檔案：
  - `index.html` / `timer.html` / `dice.html` / `scoreboard.html` / `cabin.html` / `big2.html` / `sw.js`

## 下一個目標
- 

## 方案（先寫這裡）
請先在此區提出「預計修改內容」，不要直接改程式。

### 預計修改
- 大老二加返回按鈕
	- big2.html 套用跟 timer.html / cabin.html 相同的共用返回元件（左上角圓形箭頭，回 index.html）。
- 計時器 / 骰子 / 計分表 拿掉卡片容器
	- 移除目前置中的半透明卡片（`.timer-shell` / `.dice-shell` / `.sheet` 外框＋背景），內容（標題、倒數圈/骰子/表格）直接鋪在頁面背景上，不再有邊框方塊感。
	- 操作型按鈕改為固定貼底（position: fixed; bottom: 0，含 safe-area-inset-bottom）：
		- 計時器：快速時間按鈕（30/60/2分）＋自定秒數輸入列。
		- 骰子：顆數 +/- 與「擲骰」按鈕。
		- 計分表：「新增一列」「重新開始」。
	- 上方內容區加對應的 padding-bottom，避免被固定底部列擋住。
- Safe Area 驗證方式說明（不改程式）
	- 一般 Safari 分頁瀏覽時網址列本身就會把內容往下推，`env(safe-area-inset-top)` 這時候是 0，看不出差異是正常現象；要驗證需把首頁加入主畫面、用全螢幕圖示開啟比較。
- 桌面版更新機制說明（不改程式）
	- 首頁現有的「檢查新版本」按鈕本來就同時支援桌面瀏覽器（Chrome/Edge/Safari），非手機專屬，不需另外設定。
- 山中小屋記住牌堆進度
	- cabin.html 加入 localStorage（key: `cabin_deck_v1`），只記錄「剩餘牌堆順序」。
	- 頁面載入時：若有存檔且格式正確，接續使用；沒有或格式不對則照舊洗新牌堆。
	- 每次抽牌（含牌堆抽完自動重新洗牌）都立即存檔，語音設定（語速/音調/音量/角色）不記錄，維持每次預設值。
- 全頁面限制拖移/縮放
	- 六頁（index / timer / dice / scoreboard / cabin / big2）viewport meta 都加上 `maximum-scale=1, user-scalable=no`，禁止雙指/雙擊縮放。
	- `html, body` 加 `overscroll-behavior: none`，避免整頁被拖動出現橡皮筋彈動效果；cabin.html 的翻牌拖曳手勢維持原本邏輯不受影響。

### 方案檢查（由 Copilot 填寫）
- 可能問題：
	- 計分表拿掉卡片背景後，表格本身仍需要一個可視區塊（淺色底）才能跟頁面背景分清楚欄位線，否則格線會太不明顯；預計保留表格自己的格線/底色，只拿掉外層那圈「卡片」。
	- 固定貼底的按鈕列需加 `padding-bottom: env(safe-area-inset-bottom)`，避免被 iPhone Home Indicator 擋住。
- 風險：
	- 內容區改用 padding-bottom 讓出底部按鈕高度後，若視窗很矮（如手機橫向），可能仍需捲動才看得到全部內容，這是合理的取捨。
- 替代作法：
	- 無。
- 需你確認的數值/規則：
	- 計分表的表格本身（含格線、儲存格底色）維持現有樣式，只拿掉最外層卡片框，OK 嗎？

### 架構說明（由 Copilot 填寫）
- 架構模式：
	- 三頁（timer / dice / scoreboard）統一改為「全版面內容區 + 固定底部操作列」兩段式版面：
		- 上半：`body` 直接當內容容器（不再包一層卡片 div），置中顯示核心元件（倒數圈 / 骰子 / 計分表格）。
		- 下半：`.bottom-bar`（position: fixed; bottom: 0; 含 safe-area-inset-bottom）放操作型按鈕，跨頁共用同一種樣式（半透明底、貼齊底部、不隨內容捲動）。
	- 返回按鈕（左上角圓形箭頭）維持不變，套用到 big2.html。
- 主要流程/動作：
	- 頁面載入：渲染核心內容置中 → 底部操作列固定顯示 → 內容區依底部列高度預留 padding-bottom。
	- 操作列的按鈕行為（擲骰/新增列/切換秒數等）邏輯不變，只是改變 DOM 位置與樣式（從卡片內部移到固定底部列）。

## 最後確認後再修改
- [x] 已確認方案內容
- [x] 同意開始修改

## 驗收標準
- [ ] 功能符合需求
- [ ] 不影響既有功能
- [ ] 有必要時補充測試方式

---

## 既有規範（原始內容整併）
### 一般規則
- 請都繁中回應，在下方方案處寫下要修改的地方。
- 先在方案寫下你要修改的東西，不要直接修改，不要修改其它區塊。
- 統整後一起修改。
- 幫我檢查方案的問題。
- 架構問題，請說明架構的模式和動作。
- 沒有說明的數值請列表和我確認

### 部署前檢查
- 更新首頁的版本號顯示（格式：Zm.dd#，m=月份代碼）。
- 同步更新 sw.js 第一行的 VERSION。
- 版本規則：
	- Year：26Z、27A、28B...（逐字母往下）
	- Month：1、2、3、4、5、6、7、8、9、0、J、Q
	- 備註：撲克牌邏輯 1~10、J、Q，無衝突。

### 資料欄位規則
- 有關於欄位名稱，請查 database.md。
- 若有新增欄位，也要同步加入文件。

### 頁面/連結規則
- 如果有新的頁面或連結，請說明邏輯。
- 新增或修改頁面版面時，請查 頁面架構規範.md（頁面分類、色彩/字型 Token、Safe Area 規則、新頁面模板）。
- 手機版不要 safe area
